# --- Web framework & server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database (async) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6  # SQLAlchemy Vector type for voice_examples.embedding

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Security (used from Phase 2 onward) ---
cryptography==44.0.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pinned: passlib 1.7.4 errors reading bcrypt>=4.1's removed __about__
python-jose[cryptography]==3.3.0

# --- Validation ---
email-validator==2.2.0  # pydantic EmailStr

# --- HTTP client (YouTube / LLM calls, later phases) ---
httpx==0.28.1
