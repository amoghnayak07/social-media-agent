"""LLM provider integration (bring-your-own key).

v1 supports Anthropic (Claude), OpenAI, and xAI (Grok). The agent's LLM calls
(classify/draft) live in later phases; Phase 2 only needs key validation.
"""
